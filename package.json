{
  "name": "misticakky-yt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node --import ./src/sade-theme.js --import ./src/copy-patch.js src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/gmail-utils.js && node --check src/copy-patch.js && node --check src/sade-theme.js"
  },
  "dependencies": {
    "cookie-session": "^2.1.1",
    "express": "^5.1.0",
    "googleapis": "^160.0.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
